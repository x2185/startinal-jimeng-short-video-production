# Startinal 即梦玩具短视频生产 Skill：同事使用说明

## 这个 Skill 能做什么

把一个商品的图片、实拍视频和已确认卖点，整理为默认 **20 条完整 30 秒创意方案**。Skill 会按动作复杂度选择连续的 `3 × 10 秒` 或 `6 × 5 秒` 即梦图生视频提示词（默认分别为 60 或 120 段）；可在你确认后调用火山引擎即梦 A1 视频 3.0、自动保存任务记录、下载视频、抽取衔接帧并用 FFmpeg 拼接成片。

20 条内容会按商品条件混合使用情景剧情、玩法实证、高质感展示、互动反应、钩子/反转、送礼/收藏/生活方式等模式；不会为了剧情虚构玩具功能。

## 一次性安装

1. 在仓库页面点击 **Code → Download ZIP**，解压后确认项目中存在 `skills\startinal-jimeng-short-video-production\SKILL.md`。
2. 在项目根目录运行一次安装脚本：

   ```powershell
   powershell -ExecutionPolicy Bypass -File .\skills\startinal-jimeng-short-video-production\scripts\install_for_current_user.ps1
   ```

   如果该电脑已有同名旧版 Skill，脚本会停止，不会静默覆盖。确认要更新时再追加 `-Force`。

3. 重启 Codex 桌面版或 Codex CLI。新对话中输入“使用 Startinal 即梦短视频生产 skill”，即可触发。

> 不要把 Skill 安装到商品素材文件夹中；Skill 是工具说明和脚本，商品素材应另放在每次任务的工作目录。

## 每台电脑的环境与密钥

如果从完整项目克隆/解压后使用，首次在项目目录运行以下只读环境检查：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify_team_checkout.ps1
powershell -ExecutionPolicy Bypass -File .\skills\startinal-jimeng-short-video-production\scripts\setup_video_pipeline.ps1 -Action Check
python .\skills\startinal-jimeng-short-video-production\scripts\check_environment.py --assembly
```

它会检查 Node、Python、FFmpeg、HyperFrames 和 Docker/Activepieces 状态。HyperFrames、Docker Desktop 下载和 Activepieces 启动均为可选动作，必须由使用者明确批准；Docker 安装及许可接受不会被脚本自动执行。若团队另行提供 `tools\ffmpeg` 工具包，项目会自动识别它；Git 仓库本身不提交这个大体积二进制文件。否则按检查结果自行安装 FFmpeg 或将其加入 PATH。

如果只安装本 Skill（没有完整项目目录），跳过 `..\scripts\verify_team_checkout.ps1`，从本 Skill 的 `scripts\setup_video_pipeline.ps1 -Action Check` 开始即可。

准备一个工作目录，例如：

```text
D:\JiMeng任务\玩具A\
  .env
  素材\
  output\
```

在工作目录复制项目根目录的 `.env.example` 为 `.env`，再填入该使用者获授权的密钥。推荐字段：

```dotenv
VOLCENGINE_ACCESS_KEY_ID=你的AccessKeyID
VOLCENGINE_SECRET_ACCESS_KEY=你的SecretAccessKey
```

`.env` 只能保存在使用者自己的电脑上，绝不能上传 GitHub、聊天截图、提示词文件或素材文件夹，也不得交接给其他同事。每位同事只使用、保留和修改自己获授权的 AK/SK；人员变动、疑似泄露或测试结束后，应立即由密钥所属人或管理员轮换凭据。

## 日常使用流程

在 Codex 新对话中，先进入或说明你的工作目录，再说：

```text
使用 Startinal 即梦短视频生产 skill。
素材在 D:\JiMeng任务\玩具A\素材。
请从头开始：扫描素材、建立产品身份与配件/颜色配对表、向我补齐必要信息、生成 20 条 30 秒方案和对应连续提示词；先不要提交即梦。
```

Codex 会先做素材证据审查：清晰原帧和动作前后帧比对、产品结构/颜色/配件配对表、可见与未知信息的区分。随后才会主动询问缺失的关键资料：

- 商品名称、真实玩法/使用方式；
- 已确认卖点，以及必须展示和保留的功能；
- 目标买家、市场和语言；
- 禁用内容、合规限制、人物/场景/风格偏好；
- 是否有新增或更换的素材。

请只确认真实存在的功能和卖点。若一个玩具只有一种玩法，Skill 会通过场景、目标、互动、镜头和结局做差异化，不会编造隐藏机关、灯光、声音、材质或安全认证。

## 生成前后的确认点

1. **生成提示词前**：Codex 扫描素材、建立商品身份约束和配对表，并使用默认视觉基线（真实 UGC、柔和 45° 光线、单动作、稳定结尾帧）生成 20 条方案；这一步不消耗即梦额度。
2. **付费提交前**：Codex 给出最终优化版。请明确回复要提交的方案编号，例如“确认提交 S01”。未确认时不会调用即梦 API。
3. **提交后**：每个片段按顺序生成，后段使用前段的稳定衔接帧。已提交任务、提示词、响应、片段和失败记录都会留在 `output`，中断后可用 `--resume` 恢复，不会重复提交已存在的任务。生成结果会先经过产品身份、颜色配对、手部与动作、安全限制检查；失败片段单独重做，不用剪辑掩盖。

## 成片在哪里

一次成功任务通常在工作目录中生成：

```text
output\<商品任务名>\<方案编号>-run\
  clips\clip-01\clip-01.mp4
  clips\clip-02\clip-02.mp4
  ...
  clips\clip-0N\clip-0N.mp4
  finals\package-01-30s.mp4
  manifest.json
```

最终视频在 `finals`。FFmpeg 拼接发生在本机，不消耗 GPT token 或即梦额度。

## 网络与常见问题

- 运行即梦生成、下载和拼接时，建议关闭全局 VPN；或将 `aigc-cloud.com`、`volcengineapi.com` 设为直连/分流。
- 下载失败时，脚本会尝试系统线路和绕过 HTTP(S) 代理的直连，并刷新一次短期视频链接；网络恢复后可让 Codex 用 `--resume` 继续。
- VPN 若接管全部 Windows 网络，脚本无法安全地自动关闭它；需要由电脑使用者或 IT 预先配置分流规则。
- 商品素材增删或混入其他商品时，Skill 会提示重新扫描并重建商品身份约束；不要把两个产品的素材放在同一个素材目录。
- 生成结果即使已保存，也可能出现商品变形、细节不一致、人物手指异常或剧情不成立。保留文件以便复盘，但不要把不合格片段作为下一段的参考帧。将复盘保留在当前产品输出目录；只有跨产品重复验证有效的经验才会加入共享 Skill。

## 更新 Skill

仓库更新后，重新下载 ZIP，并覆盖本机的 `startinal-jimeng-short-video-production` 文件夹，再重启 Codex。更新不会覆盖你的工作目录、素材、`output` 或 `.env`。

> Git 版本库只同步代码、Skill、脚本、说明和配置模板；商品原始素材、`output`、`materials`、本地数据库、FFmpeg 工具包与 `.env` 都不进入 Git。需要交接这些内容时，请通过公司的素材盘、对象存储或单独压缩包传递，并先确认素材使用权。

## 交付前 90 秒验收

在新电脑的项目根目录依次执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify_team_checkout.ps1 -Strict
powershell -ExecutionPolicy Bypass -File .\skills\startinal-jimeng-short-video-production\scripts\setup_video_pipeline.ps1 -Action Check
python .\skills\startinal-jimeng-short-video-production\scripts\check_environment.py --assembly
python .\skills\startinal-jimeng-short-video-production\scripts\run_jimeng_a1_package.py --help
```

前四项应分别显示总体验收、Node/FFmpeg、Python/FFmpeg，以及运行器帮助。最后一项只显示帮助，不会读取密钥、调用 API 或产生费用。Docker/Activepieces 显示 WARN 不阻塞核心流程。
