# 商品短视频生产：同事快速使用说明

这套工具的目标是：把你提供的商品素材，最后做成一条可用的 30 秒竖版短视频。

你不需要自己写复杂提示词，也不用逐段检查视频。你只需要准备真实素材、说明商品事实，并在需要付费生成时确认一次。

## 1. 第一次使用前

每台电脑只需做一次：安装 Skill、准备 FFmpeg、配置自己的火山引擎 API 密钥。安装完成后重启 Codex。

不要把 API 密钥发到聊天、GitHub 或素材群。每个人只使用自己获授权的密钥。

### API 密钥放置位置（只需配置一次）

在下载后的项目**最外层文件夹**新建一个名为 `.env` 的文件。它应和 `README.md`、`SKILL.md`、`scripts` 文件夹处在同一级，而不是放进素材文件夹、`jobs` 文件夹或 GitHub 网页。

不会判断项目在哪里时，按下面找：在资源管理器中打开你下载或解压后的项目文件夹；只要能同时看到 `README.md`、`SKILL.md` 和 `scripts`，当前文件夹就是项目最外层。点击资源管理器上方的地址栏，显示的完整位置就是你的项目位置。在这个文件夹空白处右键新建文本文件，改名为 `.env`（Windows 若提示改扩展名，选择“是”）。

最终只需确认：`.env` 和 `README.md`、`SKILL.md`、`scripts` 在同一层即可。

用记事本打开 `.env`，每行填写一项（把尖括号内容换成自己从火山引擎取得的值）：

```text
VOLCENGINE_ACCESS_KEY_ID=<你的 AccessKey ID>
VOLCENGINE_SECRET_ACCESS_KEY=<你的 Secret AccessKey>
```

保存后即可。`.env` 是本机私密文件，工具会自动读取它；不要把它上传、截图、复制到聊天，也不要改名为 `.txt`。仓库已设置为忽略这个文件。更换电脑或更换授权密钥时，只需在那台电脑重新建立自己的 `.env`。

## 2. 不需要手动建文件夹

你只需要继续使用平时的素材文件夹，例如 `D:\素材\仙女棒`。不需要建立 `source`、`jobs` 或任何子文件夹。

每次告诉工具“素材路径 + 具体款式”，它会自动在项目的 `jobs` 文件夹中建立任务、保存记录、区分候选/失败/成片。一个任务只选一个明确款式；不要让不同颜色或不同结构混在同一次任务里。

素材读取和分类完成后，如果你没有事先写“视频类型”，工具会根据已读到的素材请你从以下模式中选择一项：`商品展示`、`宣传/情节型`、`剧情模式`、`脚本优化/20 变体` 或 `其他自定义`。这一步只决定创作方向，不会调用付费生成。

## 3. 不同商品与不同款式，怎么开对话

- **换成不同商品：一定新建对话。** 每种商品都有自己的事实、素材、禁用项、失败记录和创意方案；混在一个对话会降低生成稳定性。
- **同一商品、同一具体款式：继续原对话。** 适合补素材、修改成片、继续生成或调用已验证的 20 个变体。
- **同一商品但换颜色、包装、配件或结构：建议新建对话。** 如果确实在原对话继续，必须明确写“切换到 <具体款式>”，并让工具重新读取对应素材；不能沿用上一个款式的身份图或动作证据。

## 4. 新建对话时，直接复制这段

```text
使用 $startinal-jimeng-short-video-production。

素材路径：D:\素材\商品名
本次 SKU：<颜色/款式>
目标市场与语言：<例如美国英语 TikTok>
已确认卖点：<只写真实、确认过的卖点>
必须展示：<例如产品外观、轻巧携带、真实支付片段>
视频类型：<可留空；素材分类后工具会让你选择 商品展示 / 宣传·情节型 / 剧情模式 / 脚本优化·20变体 / 其他自定义。若已有脚本，直接附上脚本>
历史 AI 成功案例：<可选，填写文件夹路径；只学习创意，不直接复用>
禁止：<例如品牌、价格、夸大承诺>

请自动找到或创建这个款式的任务记录；只分析新增或变更的素材。
目标是交付一条 30 秒成片，不要默认只给我原始片段。
除缺少关键素材、发现异常或需要付费提交确认外，不需要逐项问我。
```

如果是继续之前的任务，在最后补一句：

```text
继续上次这个款式的任务。
```

## 5. 工具会自动做什么

- 读取商品档案；有新素材时只增量分析，不会每次重新看完整个文件夹。
- 区分产品图、细节图、真实动作视频、历史 AI 成功案例、不可用素材。
- 历史 AI 成功案例只学习场景、镜头、节奏和提示词结构；不会把旧视频或旧产品外形直接混入本次成片。
- 如果选择“宣传·情节型”，先生成并审核有创意的场景首帧，再让即梦生成低风险短动态；产品外观仍以真实素材为准。
- 如果选择“剧情模式”，先围绕人物目标、情境、转折和结尾创作故事；保留一段清晰产品证明镜头。支付、装卡、旋拧等精细动作默认直接改写或省略，不会让模型生成，也不会主动要求补实拍。
- 如果选择“产品展示”，优先清晰展示外观、细节和已确认卖点，不强制添加场景剧情。
- 生成低风险场景：展示、手持、携带、桌面、材质特写、稳定产品镜头。
- 每条视频自动抽取关键帧，检查产品、手部、动作和文字风险。
- 把通过的六个低风险镜头剪成一条 30 秒成片；只有你明确指定某条已有实拍时，才会单独插入。
- 首条低风险验证通过后，自动生成 20 个创意方案预览；只有你选中编号后，才会为该编号写完整提示词并申请付费生成。
- 分开保存候选、通过、失败和最终文件。

## 6. 哪些动作默认不做

单首帧 API 不适合精细、必须真实的动作。以下内容默认不写进脚本、不生成，也不需要准备素材：

- 支付接触过程；
- 装卡、插卡、卡槽；
- 旋开、拧紧、安装、组装；
- 需要看清开始状态和结束状态的动作。

只有你在本次任务明确写“必须展示：<动作>；使用：<对应实拍素材>”时，工具才会把该条已存在的实拍作为独立插入使用。否则会直接省略该动作，并用新生成的低风险场景完成成片。不要要求它用一张产品图硬生成这些动作，容易出现产品变形、手部错误或动作不真实。

## 7. 生成结果在哪里

工具自动保存的任务记录在项目目录的：

```text
jobs\<任务名>\
  product-record\   本次读取和确认的产品资料
  scene-frames\     新生成并审核过的场景首帧
  api-ready\        每个编号的原始片段、复核帧和联系表
  <任务名>-30s.mp4  最终交付视频
  final-qa.md        最终审核记录
```

工具会在交付时直接给出最终视频链接；以该链接和任务根目录中的 `*-30s.mp4` 为准。不要把被标记为失败的候选再用于下一次生成或剪辑。

## 8. 素材更新时怎么做

- 新增同款产品图或实拍视频：继续放在原素材文件夹，新建对话时说明“素材已更新”。
- 换颜色、换包装、换配件、换结构：仍可放在素材总文件夹，但对话中必须明确本次要做哪个具体款式。
- 新增支付或安装实拍：默认不使用；只有你明确指定本次必须插入那一条时才会使用。

## 9. 什么时候需要你回复

通常只有四种情况：

1. 缺少关键产品图或真实动作素材；
2. 未给脚本或明确创意方向时，需要确认本次是“产品展示”还是“宣传·情节型”；
3. 自动检查发现产品变形、手部错误、文字/品牌风险；
4. 即将调用付费 API，需要你确认提交范围。

其余步骤由工具自动完成。

## 10. 成片不满意时怎么说

直接说清楚“不满意什么”和“想保留什么”，例如：

```text
成片不满意：场景太像产品图，缺少故事感。
保留：粉色五角星外观、装卡实拍段。
希望修改：换成送礼场景，开头先给人物和环境，不要重复桌面展示。
```

工具会保留已合格的镜头，只修改受影响的创意、提示词、片段或剪辑；本商品验证有效的偏好会记录下来，下一次同款任务会优先采用。再次调用即梦生成前仍会让你确认。
