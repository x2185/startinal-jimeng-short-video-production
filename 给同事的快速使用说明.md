# 商品短视频生产：同事快速使用说明

这套工具的目标是：把你提供的商品素材，最后做成一条可用的 30 秒竖版短视频。

你不需要自己写复杂提示词，也不用逐段检查视频。你只需要准备真实素材、说明商品事实，并在需要付费生成时确认一次。

## 1. 第一次使用前

每台电脑只需做一次：安装 Skill、准备 FFmpeg、配置自己的火山引擎 API 密钥。安装完成后重启 Codex。

不要把 API 密钥发到聊天、GitHub 或素材群。每个人只使用自己获授权的密钥。

## 2. 每个商品建立一个文件夹

一个颜色/款式就是一个独立商品文件夹，不能混放。

```text
D:\JiMeng任务\商品名\具体款式\
  source\           放原始产品图、实拍视频
  product-record\   工具自动保存的商品资料
  jobs\             每次制作一个任务文件夹
  learning-library\ 历史优质视频的学习记录
```

请把新素材直接放进 `source`。不要删除或改名已经使用过的原始素材。

## 3. 新建对话时，直接复制这段

```text
使用 $startinal-jimeng-short-video-production。

工作区：D:\JiMeng任务\商品名\具体款式
本次 SKU：<颜色/款式>
目标市场与语言：<例如美国英语 TikTok>
已确认卖点：<只写真实、确认过的卖点>
必须展示：<例如产品外观、轻巧携带、真实支付片段>
禁止：<例如品牌、价格、夸大承诺>

请先读取 product-record 和最新任务；只分析 source 中新增或变更的素材。
目标是交付一条 30 秒成片，不要默认只给我原始片段。
除缺少关键素材、发现异常或需要付费提交确认外，不需要逐项问我。
```

如果是继续之前的任务，在最后补一句：

```text
续跑任务：jobs\<日期--任务名>
```

## 4. 工具会自动做什么

- 读取商品档案；有新素材时只增量分析，不会每次重新看完整个文件夹。
- 区分产品图、细节图、真实动作视频、不可用素材。
- 生成低风险场景：展示、手持、携带、桌面、材质特写、稳定产品镜头。
- 每条视频自动抽取关键帧，检查产品、手部、动作和文字风险。
- 把通过的镜头与实拍动作镜头剪成一条 30 秒成片。
- 分开保存候选、通过、失败和最终文件。

## 5. 哪些素材一定要实拍提供

单首帧 API 不适合精细、必须真实的动作。以下内容请提供同款实拍视频：

- 支付接触过程；
- 装卡、插卡、卡槽；
- 旋开、拧紧、安装、组装；
- 需要看清开始状态和结束状态的动作。

工具会把这些实拍片段插入成片。不要要求它用一张产品图硬生成这些动作，容易出现产品变形、手部错误或动作不真实。

## 6. 生成结果在哪里

每次任务都在：

```text
jobs\<日期--任务名>\
  candidates\  正在筛选的生成片段
  accepted\    本次成片选中的镜头
  rejected\    失败片段，只用于复盘
  final\       最终交付视频 final-30s.mp4
```

最终只看 `final\final-30s.mp4`。不要把 `rejected` 中的素材再用于下一次生成或剪辑。

## 7. 素材更新时怎么做

- 新增同款产品图或实拍视频：放进 `source`，新建对话时说明“素材已更新”。
- 换颜色、换包装、换配件、换结构：新建一个具体款式文件夹，不要混进旧款。
- 新增支付或安装实拍：说明它对应哪个动作，工具会作为固定插入使用。

## 8. 什么时候需要你回复

通常只有三种情况：

1. 缺少关键产品图或真实动作素材；
2. 自动检查发现产品变形、手部错误、文字/品牌风险；
3. 即将调用付费 API，需要你确认提交范围。

其余步骤由工具自动完成。
